# users/rona/iso.nix
#
# Home Manager configuration for rona on ISO installer
#
# Features loaded automatically by mkSystem (integrated HM mode):
# - default/*: shell, dev-tools, editor, environment
# - opt-out/*: git, direnv, ssh (excluded via flake.nix features.opt-out)
#
_: {
  # ISO-specific config (features handled by mkSystem)
}
